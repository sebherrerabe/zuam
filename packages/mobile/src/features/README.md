# Mobile Feature Boundaries

Mobile implementation starts from Expo Router route files plus feature modules under `src/features/`.

Early scope is intentionally narrow:

- core task browsing
- quick capture entrypoints
- task detail
- focus entrypoints
